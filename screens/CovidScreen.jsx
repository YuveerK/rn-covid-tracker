import React from "react";
import { StyleSheet, Text, View } from "react-native";

const CovidScreen = () => {
  return (
    <View>
      <Text>I am the covid screen</Text>
    </View>
  );
};

export default CovidScreen;

const styles = StyleSheet.create({});
